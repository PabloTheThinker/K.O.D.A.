name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  build:
    name: Build wheel + sdist
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install build tooling
        run: |
          python -m pip install --upgrade pip
          pip install build twine cyclonedx-bom
      - name: Build
        # SOURCE_DATE_EPOCH pins file mtimes so repeated builds at the same git
        # commit produce byte-identical wheels + sdists — reproducible builds.
        run: |
          export SOURCE_DATE_EPOCH=$(git log -1 --pretty=%ct)
          python -m build
      - name: Validate metadata
        run: twine check dist/*
      - name: Generate SBOM (CycloneDX)
        run: |
          cyclonedx-py environment --of json -o dist/sbom.cdx.json
          cyclonedx-py environment --of xml  -o dist/sbom.cdx.xml
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
          retention-days: 7

  publish-pypi:
    name: Publish to PyPI
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    environment:
      name: pypi
      url: https://pypi.org/p/koda-security
    permissions:
      id-token: write
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/
      - name: Remove SBOM files before publish
        # PyPI action rejects non-wheel/sdist files; SBOM stays in GH release only.
        run: rm -f dist/sbom.cdx.json dist/sbom.cdx.xml
      - name: Publish
        uses: pypa/gh-action-pypi-publish@release/v1

  github-release:
    name: Create GitHub Release
    needs: publish-pypi
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/
      - name: Extract changelog section
        id: changelog
        run: |
          tag="${GITHUB_REF#refs/tags/v}"
          awk -v tag="$tag" '
            $0 ~ "^## \\[" tag "\\]" { flag=1; next }
            flag && /^## \[/ { exit }
            flag { print }
          ' CHANGELOG.md > release_notes.md
          if [ ! -s release_notes.md ]; then
            echo "K.O.D.A. v${tag}" > release_notes.md
          fi
      - name: Create release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "$GITHUB_REF_NAME" \
            --title "K.O.D.A. $GITHUB_REF_NAME" \
            --notes-file release_notes.md \
            dist/*
